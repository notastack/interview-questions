Feature: printevennumbers
  Scenario: prints all the even numbers between two limits
    Given an upper and lower limits
    When the function is called
    Then prints all the even numbers between those two limits
